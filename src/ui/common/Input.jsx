import styled from "styled-components";

// Styled component for the Input
const Input = styled.input`
  border: 1px solid var(--color-grey-300); /* Border style */
  background-color: var(--color-grey-0); /* Background color */
  border-radius: var(--border-radius-sm); /* Border radius */
  box-shadow: var(--shadow-sm); /* Box shadow */
  padding: 0.8rem 1.2rem; /* Padding */
`;

export default Input; // Exporting the Input component as default

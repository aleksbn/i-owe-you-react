import styled from "styled-components"; // Importing styled-components library

// Defining TableOperations styled component
const TableOperations = styled.div`
	display: flex; // Setting display property to flex
	align-items: center; // Aligning items along the center vertically
	gap: 1.6rem; // Specifying gap between child elements
`;

export default TableOperations; // Exporting TableOperations styled component
